<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Framework theme (no appcompat/material XML dependency); Compose draws the UI. -->
    <style name="Theme.PCRemote" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
